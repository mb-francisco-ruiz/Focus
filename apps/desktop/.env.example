# Focus desktop configuration. Copy to `apps/desktop/.env`.
# Where the desktop app (and its mini-orb / quick-capture windows) reach the
# Focus server. Baked in at build time, so set it before `pnpm --filter
# @focus/desktop tauri build`. Defaults to local dev if unset.
VITE_FOCUS_API_URL=http://localhost:3001
